# Local security scan output — NEVER commit. This repo is public and the
# contracts hold real value; findings must stay local.
/security/reports/

# Local Claude Code state and vendored audit skills (security/vendor-skills.sh).
# This is a public repo — keep all local Claude artifacts out of it.
/.claude/

# Aderyn default report locations
/report.md
/solidity/report.md

# flow
emulator-account.pkey
imports
.env

# Deployer key material + local signer config — NEVER commit (public repo, real
# funds). flow.mainnet.json references .private-key by path; the key file itself
# stays local. flow.local.json holds the bootstrap signer for account creation.
/.private-key
/.bootstrap-account-private-key
/flow.local.json

# Forge broadcast logs / deployment transaction artifacts — local only.
/solidity/broadcast/
